# Backend API base URL (no trailing slash). Required for stakeholder/project/etc. APIs.
# Example: when frontend runs on http://localhost:5000, set this so POST/GET go to Django.
VITE_API_BASE_URL=http://127.0.0.1:8000
